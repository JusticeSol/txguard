{
  "name": "txguard",
  "version": "0.1.0",
  "private": true,
  "description": "Pre-flight risk checks for AI agents. Pay-per-call x402 service on OKX.AI.",
  "scripts": {
    "test": "tsx --test tests/scoring.test.ts",
    "dev": "next dev",
    "build": "next build",
    "start": "next start"
  },
  "devDependencies": {
    "@types/node": "^26.1.1",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  }
}
